-- ============================================================================
--  seed-tax-law-how-to-ask.sql
--  ข้อมูลที่ผู้ใช้ต้องเตรียมก่อนให้ระบบคำนวณภาษี (ชุดที่ 10)
-- ----------------------------------------------------------------------------
--  เหตุผลที่ต้องมีชุดนี้
--
--    ปุ่มตัวอย่างคำถามในหน้าทักทายมีปุ่มว่า วิธีคำนวณภาษี
--    ซึ่งส่งคำถามว่า อยากคำนวณภาษีจากเงินเดือน ต้องบอกข้อมูลอะไรบ้าง
--
--    ทดลองค้นด้วยคำถามนี้ก่อนเขียน พบว่าระบบค้นเจอ 2 รายการ
--      อันดับ 1 ตัวอย่างการคำนวณภาษีเงินเดือน        ตรงประเด็น
--      อันดับ 2 เงินที่นายจ้างจ่ายครั้งเดียวเพราะออกจากงาน  ไม่เกี่ยวเลย
--    แปลว่าคำตอบต้องพึ่งความจำของแบบจำลองมากกว่าข้อมูลอ้างอิง
--    ซึ่งเป็นรูปแบบความผิดพลาดที่อันตรายที่สุดของระบบประเภทนี้
--
--    รายการนี้จึงเขียนคำตอบตรง ๆ ให้ระบบหยิบไปใช้ได้เลย
--
--  ทำไมรายการนี้ต้องสั้นเป็นพิเศษ
--    บทเรียนจากชุดที่ 8 รายการที่ยาวและมีคำสำคัญเยอะ
--    จะถูกค้นเจอติดอันดับต้นในคำถามหลายแบบ แล้วกินงบบริบท
--    จนรายการที่มีคำตอบจริงถูกตัดตกไป
--    รายการนี้จึงคุมความยาวไว้ต่ำกว่า 1,000 ตัวอักษร
--    และจงใจไม่ใส่ตัวเลขเพดานค่าลดหย่อน เพราะมีรายการอื่นที่ตอบเรื่องนั้นดีกว่าอยู่แล้ว
--
--  ผลกระทบที่วัดแล้วก่อนนำเข้า
--    ทดลองเพิ่มรายการนี้แล้วรันการค้นข้อมูลกับคำถามทดสอบ 115 ข้อ
--    จำนวนคำถามที่ผลการค้นเปลี่ยน และจำนวนคำถามที่บริบทเสียคำตอบไป
--    บันทึกไว้ในข้อความ commit ของการเปลี่ยนแปลงนี้
--
--  ปีภาษีอ้างอิง 2567
--
--  แหล่งข้อมูล
--    ประมวลรัษฎากร มาตรา 48(1) โครงสร้างการคำนวณภาษีเงินได้บุคคลธรรมดา
--    เงินได้พึงประเมิน หักค่าใช้จ่าย หักค่าลดหย่อน เป็นเงินได้สุทธิ แล้วคูณอัตราก้าวหน้า
--
--  วิธีนำเข้า
--    docker cp postgres\seed-tax-law-how-to-ask.sql tax-advisor-postgres:/tmp/ask.sql
--    docker exec -it tax-advisor-postgres psql -U n8n_admin -d tax_advisor -f /tmp/ask.sql
-- ============================================================================

DELETE FROM tax_law_knowledge WHERE source LIKE '%[ชุดที่ 10]%';

-- ---------------------------------------------------------------------------
--  ต้องใช้รูปแบบห้าคอลัมน์เหมือนไฟล์ชุดอื่นเท่านั้น
--
--  ตัวโหลดของชุดวัดผลใน evaluation/run-accuracy-test.js
--  อ่านไฟล์ seed ด้วยรูปแบบที่คาดว่ามีห้าค่าและปิดท้ายด้วยปีภาษี
--  ครั้งแรกเขียนไฟล์นี้เป็นเจ็ดคอลัมน์ ตัวโหลดจึงข้ามรายการนี้ไปเงียบ ๆ
--  ผลคือฐานข้อมูลจริงมี 98 รายการ แต่การวัดผลใช้แค่ 97 รายการ
--  ซึ่งเป็นความคลาดเคลื่อนที่ไม่มีอะไรฟ้อง เพราะสคริปต์ยังรันผ่านปกติ
--
--  ค่าของสองคอลัมน์ที่เพิ่มมาใน migration 006 ตั้งด้วยคำสั่ง UPDATE ท้ายไฟล์แทน
-- ---------------------------------------------------------------------------
INSERT INTO tax_law_knowledge (category, title, content, source, tax_year) VALUES
(
  'การคำนวณภาษี',
  'ต้องเตรียมข้อมูลอะไรบ้าง ก่อนให้ระบบคำนวณภาษีเงินได้',
  'ระบบคำนวณภาษีให้ได้เมื่อผู้ใช้บอกข้อมูลเหล่านี้ ยิ่งบอกครบ ตัวเลขยิ่งใกล้เคียงของจริง
สิ่งที่ต้องมีอย่างน้อยคือรายได้ ที่เหลือถ้าไม่บอกระบบจะคิดแบบไม่มีให้

- รายได้ ระบุด้วยว่าเป็นต่อเดือนหรือทั้งปี และเป็นเงินเดือนประจำหรือรายได้แบบอื่น
  เพราะเงินได้แต่ละประเภทหักค่าใช้จ่ายไม่เท่ากัน
- สถานภาพสมรส และคู่สมรสมีเงินได้หรือไม่
- จำนวนบุตร และบุตรคนที่สองเป็นต้นไปเกิดตั้งแต่ปี 2561 หรือไม่
- เงินสมทบประกันสังคมที่จ่ายทั้งปี
- เบี้ยประกันชีวิต ประกันสุขภาพ และประกันสุขภาพของบิดามารดา
- เงินสะสมกองทุนต่าง ๆ เช่น กองทุนสำรองเลี้ยงชีพ กบข. RMF SSF กอช.
  รวมถึงดอกเบี้ยเงินกู้ซื้อบ้าน และเงินบริจาค

ค่าใช้จ่ายไม่ต้องบอก เพราะกฎหมายกำหนดอัตราไว้แล้วตามประเภทเงินได้
ระบบหักให้เองตามประเภทที่ผู้ใช้ระบุ

ลำดับการคำนวณตามมาตรา 48(1) คือ
เงินได้พึงประเมิน ลบค่าใช้จ่าย ลบค่าลดหย่อน ได้เงินได้สุทธิ แล้วคิดตามอัตราก้าวหน้า',
  'ประมวลรัษฎากร ม.48(1) ประกอบ ม.42 ทวิ ถึง ม.46 และ ม.47 [ชุดที่ 10]',
  2567
);

-- ---------------------------------------------------------------------------
--  ตั้งค่าคอลัมน์ที่เพิ่มมาใน migration 006
-- ---------------------------------------------------------------------------
--  รายการนี้บอกว่าต้องเตรียมข้อมูลอะไร ไม่ได้มีเพดานหรืออัตราให้เอาไปคิดต่อ
--  จึงเป็นแบบตอบได้เลย และตรวจยืนยันแล้วตั้งแต่ตอนเขียน ไม่ใช่ค่าที่ระบบเดา
-- ---------------------------------------------------------------------------
UPDATE tax_law_knowledge
SET needs_calculation = false,
    calc_verified     = true
WHERE source LIKE '%[ชุดที่ 10]%';


-- ============================================================================
--  ตรวจผลหลังนำเข้า
-- ============================================================================

SELECT id, category, title, length(content) AS ความยาว, needs_calculation
FROM tax_law_knowledge
WHERE source LIKE '%[ชุดที่ 10]%';

-- จำนวนรวมต้องเป็น 98 รายการ
SELECT count(*) AS จำนวนรายการทั้งหมด FROM tax_law_knowledge;
